# functional

Vite library scaffold using TypeScript and Vitest.

## Scripts

- `npm run build` - Type-check and build the library
- `npm test` - Run tests once with Vitest
- `npm run test:watch` - Run Vitest in watch mode
